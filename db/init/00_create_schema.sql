-- ============================================================
--  Studio Student Management — Schema Creation
--  Creates the dedicated 'studio' schema and sets it as the
--  default search path for the current database.
-- ============================================================

CREATE SCHEMA IF NOT EXISTS studio;

