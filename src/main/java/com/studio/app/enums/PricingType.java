package com.studio.app.enums;

/**
 * Pricing model for a student.
 * PER_CLASS  – the student pays a fixed price per individual class.
 * PACKAGE    – the student buys a bundle of N classes for a fixed total price.
 */
public enum PricingType {
    PER_CLASS,
    PACKAGE
}
